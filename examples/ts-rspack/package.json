{
  "name": "ts-rspack-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "DEBUG=\"dts-debug\" pnpm run clean && rspack build",
    "clean": "rimraf dist && rimraf types"
  },
  "types": "dist/ts-test.d.ts",
  "devDependencies": {
    "@rspack/cli": "^1.3.9",
    "@rspack/core": "^1.3.9",
    "tslib": "^2.8.1",
    "typescript": "5.7.3"
  }
}
