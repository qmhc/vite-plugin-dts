{
  "name": "ts-rolldown-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run clean && DEBUG=\"dts-debug\" rolldown -c rolldown.config.ts",
    "clean": "rimraf dist && rimraf types"
  },
  "types": "dist/ts-test.d.ts",
  "devDependencies": {
    "rolldown": "1.0.0-beta.8",
    "tslib": "^2.8.1",
    "typescript": "5.7.3"
  }
}
