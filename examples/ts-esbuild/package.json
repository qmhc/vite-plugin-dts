{
  "name": "ts-esbuild-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run clean && DEBUG=\"dts-debug\" tsx ./build.ts",
    "clean": "rimraf dist && rimraf types"
  },
  "types": "dist/ts-test.d.ts",
  "devDependencies": {
    "esbuild": "^0.25.4",
    "tslib": "^2.8.1",
    "typescript": "5.7.3"
  }
}
