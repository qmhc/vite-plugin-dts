{
  "name": "ts-example",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "DEBUG=\"dts-debug\" pnpm run clean && webpack --config webpack.config.ts",
    "clean": "rimraf dist && rimraf types"
  },
  "types": "dist/ts-test.d.ts",
  "devDependencies": {
    "esbuild-register": "^3.6.0",
    "ts-loader": "^9.5.2",
    "tslib": "^2.8.1",
    "typescript": "5.7.3",
    "vite": "^6.0.7",
    "webpack-cli": "^6.0.1"
  }
}
